# Phase F — Tests + CI/CD

GitHub Issue: [#3](https://github.com/olgunozoktas/api-lab/issues/3)

Priority: P2

## Context

The phase that turns this from demo → maintainable project.

## Items

- [x] Zig tests (`zig build test`): http handler — argv construction, header parsing, JSON encoding, error formatting
- [ ] Vitest for stores, utils (envSubst, highlightJson, toCurl)
- [ ] E2E via zero-native automation hooks: launch app, send request, assert response shown
- [ ] `.github/workflows/ci.yml`: build + test matrix on macOS
- [ ] `.github/workflows/release.yml`: on tag push, build all platforms, GitHub Release with binaries
- [ ] Pre-commit hooks: `zig fmt`, `prettier --check`
- [ ] Cold start + bundle size benchmarks tracked in CI

## Acceptance

`zig build test` green, `dnpm run test` green, E2E green.
CI builds on macos-latest. Tag push produces a draft release.
