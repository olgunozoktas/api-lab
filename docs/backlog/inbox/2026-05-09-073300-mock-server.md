# Phase J — Built-in mock HTTP server

Define mock responses per endpoint, run from collection definitions.

- New Zig handler: `mock.start({ port, routes })`, `mock.stop`, `mock.status`
- UI: "Mock" section in sidebar, define routes by method + path + response body + status + delay
- Live indicator: requests hitting the mock server show in real time
- Use case: develop frontend before backend exists; record-replay scenarios
