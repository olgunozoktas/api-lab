# Phase G — Cross-platform (Linux + Windows)

Linux first (WebKitGTK already supported by zero-native), Windows second (WebView2).

- `app.zon`: `.platforms = .{ "macos", "linux", "windows" }`, per-platform asset paths
- Path discipline audit: any `/Users/` or backslash assumptions? `app_dirs` already cross-platform
- curl: present on macOS/Linux by default; bundle on Windows or fall back to native HTTP
- Test on each: actual VM/machine, not theoretical
- Linux: `apt`/`dnf` deps documentation (libwebkit2gtk-4.1)
- Windows: WebView2 runtime requirement note in README
