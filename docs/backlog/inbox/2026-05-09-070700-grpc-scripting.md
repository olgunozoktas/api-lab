# Phase I — gRPC + scripting

Closes the gap with full Postman feature parity.

- gRPC: spawn `grpcurl` subprocess (Zig handler, similar to http.zig). Server reflection, `.proto` file picker, unary + server-streaming
- Scripting: pre-request + test scripts via sandboxed iframe + `postMessage` (avoids QuickJS native embed)
- Restricted `pm.*` API surface (Postman compatibility shim)
- Test results panel: ✓/✗ per assertion, console.log capture
- Postman script compatibility: ~80% of `pm.environment.set`, `pm.expect`, `pm.test`
