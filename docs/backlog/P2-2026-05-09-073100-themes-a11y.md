# Phase D-extras — themes, accessibility, settings page

GitHub Issue: [#5](https://github.com/olgunozoktas/api-lab/issues/5)

Priority: P2

## Context

i18n done. Remaining Phase D items: themes, a11y, settings page.

## Items

- [x] Settings modal: proxy config, default request headers, default timeout, default TLS-verify, default redirect count, theme picker, language picker (move language out of TopBar into settings) — **partial**: timeout/redirects/TLS-verify + theme + language landed; proxy config + default headers DEFERRED (proxy needs new bridge surface; default headers needs a richer "merge with per-request" rule, separate slice).
- [x] Themes: Tokyo Night (dark), GitHub Light, Solarized Dark/Light. Each is a `@theme` block override — **partial**: Tokyo Night + GitHub Light landed (most-requested). Solarized Dark/Light DEFERRED as a follow-up; the @theme override pattern is now established so adding more is mechanical.
- [x] Theme picker shows live preview swatches — `ThemeSwatch` component renders a 4-color preview (bg / bg-elev / fg / accent) per theme.
- [x] Full keyboard navigation: every interactive element reachable via Tab, focus rings visible, Esc closes modals — global `:focus-visible` rule in main.css applies to every button/link/input/textarea/select/[tabindex]; Radix Dialog handles Esc.
- [x] ARIA labels everywhere: dialogs, dropdowns, status pills, history items — `aria-labelledby` on each settings section, `aria-label` on theme/language radiogroups + close buttons, `role="radio"` + `aria-checked` on the theme/language pills, `aria-label` on inputs.
- [ ] Screen reader announcements for response status changes (`aria-live`) — DEFERRED as a follow-up (small, well-scoped — wire `aria-live="polite"` on the status pill in ResponseHead).
- [ ] High-contrast theme variant for accessibility — DEFERRED as a follow-up (the @theme override pattern is now in place; high-contrast just adds another `:root[data-theme="high-contrast"]` block).

## Follow-ups (deferred — file stays in queue)

- **Proxy config + default headers in Settings**: proxy needs a new field on the http.request bridge contract (forwarded to curl as `--proxy <url>`). Default headers needs UI for an "always merge" header list and a rule for collisions with per-request headers.
- **Solarized Dark / Light themes**: just add two more `:root[data-theme="solarized-{dark,light}"]` blocks in `main.css` plus `THEMES` array entries + i18n labels.
- **`aria-live` on response status changes**: add `aria-live="polite"` to the status pill in `ResponseHead.tsx` so screen readers announce when a response lands.
- **High-contrast theme variant**: another `:root[data-theme="high-contrast"]` block with WCAG AAA contrast pairs.
