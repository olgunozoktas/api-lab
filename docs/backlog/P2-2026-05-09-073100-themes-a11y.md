# Phase D-extras — themes, accessibility, settings page

Priority: P2

## Context

i18n done. Remaining Phase D items: themes, a11y, settings page.

## Items

- [ ] Settings modal: proxy config, default request headers, default timeout, default TLS-verify, default redirect count, theme picker, language picker (move language out of TopBar into settings)
- [ ] Themes: Tokyo Night (dark), GitHub Light, Solarized Dark/Light. Each is a `@theme` block override
- [ ] Theme picker shows live preview swatches
- [ ] Full keyboard navigation: every interactive element reachable via Tab, focus rings visible, Esc closes modals
- [ ] ARIA labels everywhere: dialogs, dropdowns, status pills, history items
- [ ] Screen reader announcements for response status changes (`aria-live`)
- [ ] High-contrast theme variant for accessibility
